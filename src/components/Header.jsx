import React from "react";

const Header = () => {
    return (
        <header>
            <h1>Portfolio</h1>
            <nav>
                <a href="#about">O mnie</a>
                <a href="#projects">Projekty</a>
            </nav>
        </header>
    );
};

export default Header;